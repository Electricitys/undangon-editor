"use client";

import React from "react";
import { AuthPage as AuthPageBase, AuthProps } from "@refinedev/mantine";

export const AuthPage = (props: AuthProps) => {
  return <AuthPageBase {...props} />;
};
